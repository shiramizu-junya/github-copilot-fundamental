<!DOCTYPE html>
<html lang="ja">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ToDo新規作成</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <div class="container">
        <header>
            <h1>📝 ToDo新規作成</h1>
            <a href="/" class="btn btn-secondary">← 一覧に戻る</a>
        </header>

        <main>
            <form action="/todos" method="POST" class="todo-form">
                <div class="form-group">
                    <label for="title">タイトル <span class="required">*</span></label>
                    <input type="text" id="title" name="title" required placeholder="タイトルを入力">
                </div>

                <div class="form-group">
                    <label for="content">内容</label>
                    <textarea id="content" name="content" rows="4" placeholder="内容を入力"></textarea>
                </div>

                <div class="form-group">
                    <label for="due_date">期限</label>
                    <input type="date" id="due_date" name="due_date">
                </div>

                <div class="form-group">
                    <label for="priority">優先度</label>
                    <select id="priority" name="priority">
                        <% Object.entries(priorityLabels).forEach(([value, label])=> { %>
                            <option value="<%= value %>" <%=value==='2' ? 'selected' : '' %>><%= label %>
                            </option>
                            <% }); %>
                    </select>
                </div>

                <div class="form-actions">
                    <button type="submit" class="btn btn-primary">作成</button>
                    <a href="/" class="btn btn-secondary">キャンセル</a>
                </div>
            </form>
        </main>
    </div>
</body>

</html>
