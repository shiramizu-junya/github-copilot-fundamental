<!DOCTYPE html>
<html lang="ja">

<head>
    <%- include('partials/head') %>
    <title>ToDo一覧</title>
</head>

<body>
    <%- include('partials/navbar') %>

    <div class="container">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1 class="h3 mb-0">ToDo一覧</h1>
            <a href="/todos/new" class="btn btn-primary">
                <i class="bi bi-plus-lg"></i> 新規作成
            </a>
        </div>

        <main>
            <% if (todos.length === 0) { %>
                <div class="alert alert-info text-center" role="alert">
                    <i class="bi bi-info-circle me-2"></i>
                    ToDoがありません。新規作成してください。
                </div>
            <% } else { %>
                <%- include('partials/sort-controls', { currentSort: currentSort, currentOrder: currentOrder }) %>
                <%- include('partials/todo-table', { todos: todos }) %>
                <%- include('partials/todo-cards', { todos: todos }) %>
            <% } %>
        </main>
    </div>

    <%- include('partials/footer') %>
</body>

</html>
