<!DOCTYPE html>
<html lang="ja">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ToDo一覧</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-4">
        <div class="container">
            <a class="navbar-brand" href="/">📝 ToDo管理</a>
        </div>
    </nav>

    <div class="container">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1 class="h3 mb-0">ToDo一覧</h1>
            <a href="/todos/new" class="btn btn-primary">
                <i class="bi bi-plus-lg"></i> 新規作成
            </a>
        </div>

        <main>
            <% if (todos.length===0) { %>
                <div class="alert alert-info text-center" role="alert">
                    <i class="bi bi-info-circle me-2"></i>
                    ToDoがありません。新規作成してください。
                </div>
                <% } else { %>
                    <!-- ソートコントロール -->
                    <div class="card mb-3">
                        <div class="card-body py-2">
                            <div class="d-flex flex-wrap align-items-center gap-2">
                                <span class="text-muted small">並び替え:</span>
                                <a href="/?sort=due_date&order=<%= currentSort === 'due_date' && currentOrder === 'asc' ? 'desc' : 'asc' %>"
                                    class="btn btn-sm <%= currentSort === 'due_date' ? 'btn-primary' : 'btn-outline-secondary' %>">
                                    期限 <% if (currentSort==='due_date' ) { %><i
                                            class="bi bi-arrow-<%= currentOrder === 'asc' ? 'up' : 'down' %>"></i>
                                        <% } %>
                                </a>
                                <a href="/?sort=priority&order=<%= currentSort === 'priority' && currentOrder === 'asc' ? 'desc' : 'asc' %>"
                                    class="btn btn-sm <%= currentSort === 'priority' ? 'btn-primary' : 'btn-outline-secondary' %>">
                                    優先度 <% if (currentSort==='priority' ) { %><i
                                            class="bi bi-arrow-<%= currentOrder === 'asc' ? 'up' : 'down' %>"></i>
                                        <% } %>
                                </a>
                                <a href="/?sort=created_at&order=<%= currentSort === 'created_at' && currentOrder === 'asc' ? 'desc' : 'asc' %>"
                                    class="btn btn-sm <%= currentSort === 'created_at' ? 'btn-primary' : 'btn-outline-secondary' %>">
                                    作成日 <% if (currentSort==='created_at' ) { %><i
                                            class="bi bi-arrow-<%= currentOrder === 'asc' ? 'up' : 'down' %>"></i>
                                        <% } %>
                                </a>
                            </div>
                        </div>
                    </div>

                    <!-- PC表示: テーブル -->
                    <div class="table-responsive d-none d-md-block">
                        <table class="table table-hover align-middle">
                            <thead class="table-dark">
                                <tr>
                                    <th>タイトル</th>
                                    <th>内容</th>
                                    <th>期限</th>
                                    <th>優先度</th>
                                    <th class="text-center">操作</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% todos.forEach(todo=> { %>
                                    <tr>
                                        <td class="fw-semibold">
                                            <%= todo.title %>
                                        </td>
                                        <td class="text-muted text-truncate" style="max-width: 200px;">
                                            <%= todo.content || '-' %>
                                        </td>
                                        <td>
                                            <% if (todo.due_date) { %>
                                                <i class="bi bi-calendar-event me-1"></i>
                                                <%= todo.due_date %>
                                                    <% } else { %>
                                                        <span class="text-muted">-</span>
                                                        <% } %>
                                        </td>
                                        <td>
                                            <% if (todo.priority==1) { %>
                                                <span class="badge bg-danger">高</span>
                                                <% } else if (todo.priority==2) { %>
                                                    <span class="badge bg-warning text-dark">中</span>
                                                    <% } else { %>
                                                        <span class="badge bg-success">低</span>
                                                        <% } %>
                                        </td>
                                        <td class="text-center">
                                            <a href="/todos/<%= todo.id %>/edit"
                                                class="btn btn-sm btn-outline-warning me-1">
                                                <i class="bi bi-pencil"></i>
                                            </a>
                                            <form action="/todos/<%= todo.id %>/delete" method="POST" class="d-inline"
                                                onsubmit="return confirm('削除してもよろしいですか？');">
                                                <button type="submit" class="btn btn-sm btn-outline-danger">
                                                    <i class="bi bi-trash"></i>
                                                </button>
                                            </form>
                                        </td>
                                    </tr>
                                    <% }); %>
                            </tbody>
                        </table>
                    </div>

                    <!-- モバイル表示: カード -->
                    <div class="d-md-none">
                        <% todos.forEach(todo=> { %>
                            <div class="card mb-3">
                                <div class="card-body">
                                    <div class="d-flex justify-content-between align-items-start mb-2">
                                        <h5 class="card-title mb-0">
                                            <%= todo.title %>
                                        </h5>
                                        <% if (todo.priority==1) { %>
                                            <span class="badge bg-danger">高</span>
                                            <% } else if (todo.priority==2) { %>
                                                <span class="badge bg-warning text-dark">中</span>
                                                <% } else { %>
                                                    <span class="badge bg-success">低</span>
                                                    <% } %>
                                    </div>
                                    <% if (todo.content) { %>
                                        <p class="card-text text-muted small">
                                            <%= todo.content %>
                                        </p>
                                        <% } %>
                                            <% if (todo.due_date) { %>
                                                <p class="card-text small">
                                                    <i class="bi bi-calendar-event me-1"></i>
                                                    <%= todo.due_date %>
                                                </p>
                                                <% } %>
                                                    <div class="d-flex gap-2 mt-3">
                                                        <a href="/todos/<%= todo.id %>/edit"
                                                            class="btn btn-sm btn-warning flex-grow-1">
                                                            <i class="bi bi-pencil me-1"></i>編集
                                                        </a>
                                                        <form action="/todos/<%= todo.id %>/delete" method="POST"
                                                            class="flex-grow-1"
                                                            onsubmit="return confirm('削除してもよろしいですか？');">
                                                            <button type="submit" class="btn btn-sm btn-danger w-100">
                                                                <i class="bi bi-trash me-1"></i>削除
                                                            </button>
                                                        </form>
                                                    </div>
                                </div>
                            </div>
                            <% }); %>
                    </div>
                    <% } %>
        </main>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
